[user]
  name = Alan Palazzolo
	email = 190733+zzolo@users.noreply.github.com
	signingkey = 50EA1DF85CFB3BBD93932FD55D9DF10C8E024FA3
[core]
  editor = nano
  excludesfile = /Users/zzolo/.gitignore_global
  quotepath = false
[color]
  diff = auto
  status = auto
[color "diff"]
  meta = blue bold
  frag = magenta bold
  old = red
  new = green
[color "status"]
  added = green
  changed = blue bold
  untracked = red
[alias]
  st = status -sb
  sb = status -sb
  aa = add -A
  br = branch
  ci = commit
  co = checkout
  df = diff
  dc = diff --cached
  last = log -1 HEAD
  lg = log --graph --decorate --pretty=format:'%C(magenta)%h%Creset -%C(red)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
  tree = log --graph --decorate --pretty=format:'%C(magenta)%h%Creset -%C(red)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --date=relative
  today = diff --stat @{yesterday}..HEAD
[github]
  user = zzolo
[credential]
  helper = osxkeychain
[init]
	defaultBranch = main
[pull]
	rebase = false
[commit]
	gpgsign = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
